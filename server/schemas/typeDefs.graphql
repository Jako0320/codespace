const typeDefs = `
    type User {
        _id: ID!
        username: String!
        email: String!
        password: String!
        posts: String!
        comments: [Posts]!
        # add more here, check models
    }

    type Auth {
        token: ID!
        user: User!
    }

    type Query {
        me: User
    }

`;

module.exports = typeDefs;
